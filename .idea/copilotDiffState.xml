<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/src/main/java/com/eclipse/ahorcado/proyecto1/controllers/GameController.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/com/eclipse/ahorcado/proyecto1/controllers/GameController.java" />
              <option name="originalContent" value="package com.eclipse.ahorcado.proyecto1.controllers;&#10;&#10;import com.eclipse.ahorcado.proyecto1.models.GameModel;&#10;import javafx.fxml.FXML;&#10;import javafx.scene.control.TextField;&#10;import javafx.scene.control.Label;&#10;import javafx.scene.layout.HBox;&#10;import java.text.Normalizer;&#10;import java.util.ArrayList;&#10;import java.util.List;&#10;&#10;public class GameController {&#10;&#10;    @FXML&#10;    private TextField letterInput;&#10;&#10;    @FXML&#10;    private Label feedbackLabel;&#10;&#10;    @FXML&#10;    private HBox wordContainer; // Contenedor para los campos dinamicos&#10;&#10;    private GameModel model;&#10;    private List&lt;TextField&gt; letterFields; // Guarda las referencias a los campos generados&#10;&#10;    @FXML&#10;    public void initialize() {&#10;        this.letterFields = new ArrayList&lt;&gt;();&#10;        // this.model = new GameModel(); // Borra esto de aquí, lo inicializamos cuando llegue la palabra&#10;        // generateWordFields(); // Borra esto también&#10;    }&#10;&#10;    // NUEVO MÉTODO: Lo llama el InicioController&#10;    public void iniciarJuegoConPalabra(String palabraSecreta) {&#10;        // Debes asegurar que tu GameModel pueda recibir esta palabra en el constructor o en un setter&#10;        this.model = new GameModel();&#10;        this.model.setSecretWord(palabraSecreta); // &lt;-- Asegúrate de tener este método en tu GameModel&#10;&#10;        generateWordFields();&#10;    }&#10;&#10;    // Genera un TextField bloqueado por cada letra de la palabra secreta&#10;   private void generateWordFields() {&#10;        int wordLength = model.getSecretWord().length();&#10;&#10;        for (int i = 0; i &lt; wordLength; i++) {&#10;            TextField field = new TextField();&#10;            field.setPrefWidth(40);&#10;            field.setEditable(false); // El usuario no escribe directamente aqui&#10;            field.setStyle(&quot;-fx-alignment: center; -fx-font-size: 16px;&quot;);&#10;&#10;            letterFields.add(field);&#10;            wordContainer.getChildren().add(field);&#10;        }&#10;    }&#10;&#10;    @FXML&#10;    public void handleVerifyLetter() {&#10;        String input = letterInput.getText();&#10;&#10;        if (input == null || input.trim().length() != 1) {&#10;            feedbackLabel.setText(&quot;Entrada invalida. Ingrese solo una letra.&quot;);&#10;            return;&#10;        }&#10;&#10;        char cleanLetter = formatInputChar(input);&#10;        boolean isValid = model.isLetterValid(cleanLetter);&#10;&#10;        if (isValid) {&#10;            feedbackLabel.setText(&quot;La letra '&quot; + cleanLetter + &quot;' es valida.&quot;);&#10;            updateVisibleLetters(cleanLetter);&#10;        } else {&#10;            feedbackLabel.setText(&quot;La letra '&quot; + cleanLetter + &quot;' es incorrecta.&quot;);&#10;        }&#10;&#10;        letterInput.clear();&#10;    }&#10;&#10;    // Revela la letra en los campos correspondientes usando su posicion&#10;    private void updateVisibleLetters(char cleanLetter) {&#10;        List&lt;Integer&gt; positions = model.getLetterPositions(cleanLetter);&#10;        String originalWord = model.getSecretWord();&#10;&#10;        for (int pos : positions) {&#10;            // Obtenemos la letra original (con acento si lo tenia) basandonos en la posicion&#10;            char originalChar = originalWord.charAt(pos);&#10;            letterFields.get(pos).setText(String.valueOf(originalChar));&#10;        }&#10;    }&#10;&#10;    private char formatInputChar(String input) {&#10;        String normalized = Normalizer.normalize(input, Normalizer.Form.NFD);&#10;        String withoutAccents = normalized.replaceAll(&quot;\\p{M}&quot;, &quot;&quot;);&#10;        return withoutAccents.toUpperCase().charAt(0);&#10;    }&#10;}" />
              <option name="updatedContent" value="package com.eclipse.ahorcado.proyecto1.controllers;&#13;&#10;&#13;&#10;import com.eclipse.ahorcado.proyecto1.models.GameModel;&#13;&#10;import javafx.fxml.FXML;&#13;&#10;import javafx.scene.control.TextField;&#13;&#10;import javafx.scene.control.Label;&#13;&#10;import javafx.scene.layout.HBox;&#13;&#10;import java.text.Normalizer;&#13;&#10;import java.util.ArrayList;&#13;&#10;import java.util.List;&#13;&#10;&#13;&#10;public class GameController {&#13;&#10;&#13;&#10;    @FXML&#13;&#10;    private TextField letterInput;&#13;&#10;&#13;&#10;    @FXML&#13;&#10;    private Label feedbackLabel;&#13;&#10;&#13;&#10;    @FXML&#13;&#10;    private HBox wordContainer; // Contenedor para los campos dinamicos&#13;&#10;&#13;&#10;    private GameModel model;&#13;&#10;    private List&lt;TextField&gt; letterFields; // Guarda las referencias a los campos generados&#13;&#10;&#13;&#10;    @FXML&#13;&#10;    public void initialize() {&#13;&#10;        this.letterFields = new ArrayList&lt;&gt;();&#13;&#10;        // this.model = new GameModel(); // Borra esto de aquí, lo inicializamos cuando llegue la palabra&#13;&#10;        // generateWordFields(); // Borra esto también&#13;&#10;    }&#13;&#10;&#13;&#10;    // NUEVO MÉTODO: Lo llama el InicioController&#13;&#10;    public void iniciarJuegoConPalabra(String palabraSecreta) {&#13;&#10;        // Debes asegurar que tu GameModel pueda recibir esta palabra en el constructor o en un setter&#13;&#10;        this.model = new GameModel();&#13;&#10;        this.model.setSecretWord(palabraSecreta); // &lt;-- Asegúrate de tener este método en tu GameModel&#13;&#10;&#13;&#10;        generateWordFields();&#13;&#10;    }&#13;&#10;&#13;&#10;    // Genera un TextField bloqueado por cada letra de la palabra secreta&#10;    private void generateWordFields() {&#10;        // Limpiar los campos anteriores si existen&#10;        letterFields.clear();&#10;        wordContainer.getChildren().clear();&#10;        &#10;        int wordLength = model.getSecretWord().length();&#10;        System.out.println(&quot;Palabra secreta: &quot; + model.getSecretWord() + &quot;, Longitud: &quot; + wordLength);&#10;&#10;        for (int i = 0; i &lt; wordLength; i++) {&#10;            TextField field = new TextField();&#10;            field.setPrefWidth(40);&#10;            field.setEditable(false); // El usuario no escribe directamente aqui&#10;            field.setStyle(&quot;-fx-alignment: center; -fx-font-size: 16px;&quot;);&#10;&#10;            letterFields.add(field);&#10;            wordContainer.getChildren().add(field);&#10;        }&#10;    }&#13;&#10;&#13;&#10;    @FXML&#13;&#10;    public void handleVerifyLetter() {&#13;&#10;        String input = letterInput.getText();&#13;&#10;&#13;&#10;        if (input == null || input.trim().length() != 1) {&#13;&#10;            feedbackLabel.setText(&quot;Entrada invalida. Ingrese solo una letra.&quot;);&#13;&#10;            return;&#13;&#10;        }&#13;&#10;&#13;&#10;        char cleanLetter = formatInputChar(input);&#13;&#10;        boolean isValid = model.isLetterValid(cleanLetter);&#13;&#10;&#13;&#10;        if (isValid) {&#13;&#10;            feedbackLabel.setText(&quot;La letra '&quot; + cleanLetter + &quot;' es valida.&quot;);&#13;&#10;            updateVisibleLetters(cleanLetter);&#13;&#10;        } else {&#13;&#10;            feedbackLabel.setText(&quot;La letra '&quot; + cleanLetter + &quot;' es incorrecta.&quot;);&#13;&#10;        }&#13;&#10;&#13;&#10;        letterInput.clear();&#13;&#10;    }&#13;&#10;&#13;&#10;    // Revela la letra en los campos correspondientes usando su posicion&#13;&#10;    private void updateVisibleLetters(char cleanLetter) {&#13;&#10;        List&lt;Integer&gt; positions = model.getLetterPositions(cleanLetter);&#13;&#10;        String originalWord = model.getSecretWord();&#13;&#10;&#13;&#10;        for (int pos : positions) {&#13;&#10;            // Obtenemos la letra original (con acento si lo tenia) basandonos en la posicion&#13;&#10;            char originalChar = originalWord.charAt(pos);&#13;&#10;            letterFields.get(pos).setText(String.valueOf(originalChar));&#13;&#10;        }&#13;&#10;    }&#13;&#10;&#13;&#10;    private char formatInputChar(String input) {&#13;&#10;        String normalized = Normalizer.normalize(input, Normalizer.Form.NFD);&#13;&#10;        String withoutAccents = normalized.replaceAll(&quot;\\p{M}&quot;, &quot;&quot;);&#13;&#10;        return withoutAccents.toUpperCase().charAt(0);&#13;&#10;    }&#13;&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/main/java/com/eclipse/ahorcado/proyecto1/models/GameModel.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/com/eclipse/ahorcado/proyecto1/models/GameModel.java" />
              <option name="originalContent" value="package com.eclipse.ahorcado.proyecto1.models;&#10;&#10;import com.eclipse.ahorcado.proyecto1.models.GameModel;&#10;import java.text.Normalizer;&#10;import java.util.ArrayList;&#10;import java.util.List;&#10;&#10;public class GameModel {&#10;    private String secretWord;&#10;    private String normalizedSecretWord;&#10;&#10;    public GameModel() {&#10;        this.secretWord = &quot;ÁRBOL&quot;;&#10;        this.normalizedSecretWord = removeAccents(this.secretWord).toUpperCase();&#10;    }&#10;&#10;    public void setSecretWord(String secretWord) {&#10;        this.secretWord = secretWord;&#10;    }&#10;&#10;    // 3. Método para leer la palabra (que ya usas en tu GameController)&#10;    public String getSecretWord() {&#10;        return this.secretWord;&#10;    }&#10;&#10;    // Retorna una lista con los indices donde aparece la letra adivinada&#10;    public List&lt;Integer&gt; getLetterPositions(char letter) {&#10;        List&lt;Integer&gt; positions = new ArrayList&lt;&gt;();&#10;        for (int i = 0; i &lt; this.normalizedSecretWord.length(); i++) {&#10;            if (this.normalizedSecretWord.charAt(i) == letter) {&#10;                positions.add(i);&#10;            }&#10;        }&#10;        return positions;&#10;    }&#10;&#10;    public boolean isLetterValid(char letter) {&#10;        return this.normalizedSecretWord.indexOf(letter) &gt;= 0;&#10;    }&#10;&#10;    private String removeAccents(String text) {&#10;        String normalized = Normalizer.normalize(text, Normalizer.Form.NFD);&#10;        return normalized.replaceAll(&quot;\\p{M}&quot;, &quot;&quot;);&#10;    }&#10;}" />
              <option name="updatedContent" value="package com.eclipse.ahorcado.proyecto1.models;&#10;&#10;import com.eclipse.ahorcado.proyecto1.models.GameModel;&#10;import java.text.Normalizer;&#10;import java.util.ArrayList;&#10;import java.util.List;&#10;&#10;public class GameModel {&#10;    private String secretWord;&#10;    private String normalizedSecretWord;&#10;&#10;    public GameModel() {&#10;        this.secretWord = &quot;ÁRBOL&quot;;&#10;        this.normalizedSecretWord = removeAccents(this.secretWord).toUpperCase();&#10;    }&#10;&#10;    public void setSecretWord(String secretWord) {&#10;        this.secretWord = secretWord;&#10;        this.normalizedSecretWord = removeAccents(this.secretWord).toUpperCase();&#10;    }&#10;&#10;    // 3. Método para leer la palabra (que ya usas en tu GameController)&#10;    public String getSecretWord() {&#10;        return this.secretWord;&#10;    }&#10;&#10;    // Retorna una lista con los indices donde aparece la letra adivinada&#10;    public List&lt;Integer&gt; getLetterPositions(char letter) {&#10;        List&lt;Integer&gt; positions = new ArrayList&lt;&gt;();&#10;        for (int i = 0; i &lt; this.normalizedSecretWord.length(); i++) {&#10;            if (this.normalizedSecretWord.charAt(i) == letter) {&#10;                positions.add(i);&#10;            }&#10;        }&#10;        return positions;&#10;    }&#10;&#10;    public boolean isLetterValid(char letter) {&#10;        return this.normalizedSecretWord.indexOf(letter) &gt;= 0;&#10;    }&#10;&#10;    private String removeAccents(String text) {&#10;        String normalized = Normalizer.normalize(text, Normalizer.Form.NFD);&#10;        return normalized.replaceAll(&quot;\\p{M}&quot;, &quot;&quot;);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>